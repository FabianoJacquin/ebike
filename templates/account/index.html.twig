{% extends 'base.html.twig' %}

{% block innerPage %}

    <div class="hero inner-page" style="background-image: url('{{ asset('assets/images/hero_1_a.jpg') }}');">

        <div class="container">
            <div class="row align-items-end ">
                <div class="col-lg-5">

                    <div class="intro">
                        <h1><strong>Account</strong></h1>
                        <div class="custom-breadcrumbs"><a href="{{ path('home') }}">Home</a> <span class="mx-2">/</span> <strong>Account</strong></div>
                    </div>

                </div>
            </div>
        </div>
    </div>

{% endblock %}

{% block content %}
    Ciao {{ app.user.firstname }}, benvenuto nella tua pagina privata.
    <hr>
    <a href="{{ path('account_password') }}">Modificare la password</a>
    <br>
{% endblock %}
